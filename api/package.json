{
  "name": "pollution-api",
  "version": "1.0.0",
  "scripts": {
    "start": "json-server --watch db.json --port $PORT --host 0.0.0.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "json-server": "^0.17.4"
  }
}